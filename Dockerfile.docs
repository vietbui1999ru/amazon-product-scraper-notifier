FROM python:3.12-slim AS builder
WORKDIR /build
COPY mkdocs.yml .
COPY docs/ docs/
RUN pip install --no-cache-dir mkdocs-material==9.5.18
RUN mkdocs build --strict

FROM nginx:alpine
COPY --from=builder /build/site /usr/share/nginx/html
# Caddy handles headers; nginx just serves files
EXPOSE 80
